function solve(input) {
    var cel = input;
    cel.forEach = 
    for(i = 1; i <= input; i++) {
        
        let far = Math.round(cel * 5 / 9);
        console.log(far);
    }
}

solve(0, 15, 30)